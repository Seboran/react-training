import SwipeableViews from "react-swipeable-views";
import { bindKeyboard } from "react-swipeable-views-utils";

const SwipeableKeyboard = bindKeyboard(SwipeableViews);
export default SwipeableKeyboard;